import glob
import os
from setuptools import setup

os.chdir(os.path.dirname(os.path.abspath(__file__)))

setup(
    name='task_maker',
    version='${PROJECT_VERSION}',
    packages=[
        'task_maker', 'task_maker.uis', 'task_maker.proto', 'task_maker.bin'
    ],
    zip_safe=False,
    package_data={
        'task_maker.bin': ['*'],
    },
    entry_points={
        "console_scripts": ["task-maker = task_maker.task_maker:main"]
    })
